1. Fix overflow on ui so that it overflows correctly. As seen on view webpages
2. Add notification flows.
3. Decouple the guard processing from monitor service to a transactional service so it can be ran manually upon user selection.
4. Add a button on guard detail that allows running guard manually
5. Add notification support for get and post requests and maybe a twilio sms provider
6. Add a button to trigger notification manually
7. Use Trim() on backend when creating entities

