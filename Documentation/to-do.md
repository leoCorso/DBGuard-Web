2. Add notification flows.
5. Add notification support for get and post requests and maybe a twilio sms provider
6. Add a button to trigger notification manually
7. Use Trim() on backend when creating entities
8. Add loading state to all components