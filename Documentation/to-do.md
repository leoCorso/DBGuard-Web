# To do




### 1.1.1
* Add database name to database connections table
* Add trim() when guard is validated. The trigger column has issues when a space is added.


### 1.2.0

* Add closed hours that can be specified for each day
* Ask to record analytics data and record analytics if granted
* Add a run after datetime for guards where it wont execute until a given time
* Add a web worker to handle idle detection so that it works when tab is not selected

